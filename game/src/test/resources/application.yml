quizServerPath: http://localhost:8099/quizzes

spring:
  jpa:
    show-sql: true
  session:
    store-type: hash_map
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.session.SessionAutoConfiguration

security:
  basic:
    enabled: false

eureka:
  client:
    enabled: false